# Frontend environment variables
# Copy to .env.local for local development (not committed to git)

# API base path. In development, Vite proxies /api to http://localhost:8000
# via the proxy configured in vite.config.ts.
# For production builds, set this to your backend URL (e.g., https://api.example.com)
VITE_API_BASE=/api